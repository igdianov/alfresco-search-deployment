{{- if .Values.slave.ingress.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "alfresco-search.fullName" . }}-solr-slave
  labels:
    # FIXME: Need to be fixed for the networkpolicies
    app: {{ template "alfresco-search.host" . }}
    release: {{ .Release.Name }}
type: Opaque
data:
  auth: {{ .Values.common.ingress.basicAuth | quote }}
{{- end }}